export interface ProductCard {
    id: string;
    title: string;
    price: number;
    image: string;
    artistName: string;

    // categoryId?: number;
    // subCategoryId?: number;
    // themeId?: number;

    // product or product card ??
}
